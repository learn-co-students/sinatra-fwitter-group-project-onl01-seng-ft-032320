<p> Welcome, <%= current_user.username %>
<h2> Tweets </h2>

<% @tweets.each do |tweet| %>
  <div>
    <a href="/tweets/<%= tweet. id%>"><%= tweet.content %></a>
  </div>
<% end %>
<br>

<a href="/tweets/new">Create a tweet</a> or <a href="/logout">Logout?</a>